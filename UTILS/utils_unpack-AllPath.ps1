# Powershell script that uses 7-zip to run through the target directory
# finding all types of rar files in all folders recursively and unrars them into the files folder location.


function unrar-fodler {
    
   
}
